name: rpg
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  chat_gpt_sdk: ^2.1.5
  flutter_riverpod: ^2.3.6
  animated_text_kit: ^4.2.2
  lottie: ^2.3.2
  fluttertoast: ^8.2.2
  envied: ^0.3.0+3
  shared_preferences: ^2.1.1
  animations: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  envied_generator: ^0.3.0
  build_runner: ^2.4.4

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/loader-cat.json
    - assets/lottie/dog.json
    - assets/lottie/sword.json
    - assets/lottie/setting.json
    - assets/lottie/chat.json
    - assets/lottie/breakfast.json
    - assets/lottie/island.json
    - assets/lottie/felix.json
    - assets/lottie/cute-tiger.json
    - assets/lottie/rikka.json
